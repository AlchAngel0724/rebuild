# Contributing

This repo is a rebuild effort.

Ground rules:
- Do not modify `reference/` unless explicitly requested.
- Keep changes scoped; avoid refactors mixed with fixes.
- Follow the standards in `docs/sqlite-standards.md` and `docs/queue-standards.md`.
- Never commit secrets.
