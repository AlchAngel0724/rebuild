# Rebuild Workspace

This directory will contain the new implementation.

Rules:
- Do not copy/paste private keys or secrets.
- Follow local-first defaults (no Docker required).
- Follow SQLite + job queue standards under `docs/`.
